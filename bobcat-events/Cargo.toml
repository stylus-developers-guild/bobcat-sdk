[package]
name = "bobcat-events"
version = "0.6.3"
edition = "2024"

[dependencies]
array-concat.workspace = true
bobcat-maths = { version = "0.6.3", path = "../bobcat-maths" }

[features]
alloc = []
