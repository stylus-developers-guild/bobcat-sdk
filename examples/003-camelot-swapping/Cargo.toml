[package]
name = "bobcat-sdk-examples-003"
version = "0.1.0"
edition = "2024"

[dependencies]
bobcat-sdk = { workspace = true, features = ["panic", "alloc"] }
const-hex = { version = "1.17.0", default-features = false }

