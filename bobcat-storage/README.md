
# bobcat-storage

Storage accessors and writers using the storage and transient storage tree. `mutex` can be
configured to allow multiple thread access to the underlying static memory powering the
local implementation.
